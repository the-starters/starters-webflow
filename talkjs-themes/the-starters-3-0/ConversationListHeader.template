<template>
  <div class="conversation-list-header">
    <span class="conversation-list-title">Inbox</span>
    <div class="feed-filters" role="group" aria-label="Conversation filters">
      <ActionButton action="messages-filter-all">All</ActionButton>
      <ActionButton action="messages-filter-unread">Unread</ActionButton>
      <ActionButton action="messages-filter-read">Read</ActionButton>
    </div>
  </div>
</template>
<style scoped>
/* The title row is its own band with its own bottom rule, rather than the whole
   header carrying one rule under the filters. That puts the line between
   "Inbox" and the filters, at exactly the height of ChatHeader's own rule, so
   the two run together as one horizontal grid line across the pane. Height is
   set explicitly (not derived from the title's line-height plus padding)
   because it has to equal .ChatHeader__header's 5.25rem to the pixel — if that
   value ever changes, these two must change together. */
.conversation-list-header {
  display: flex;
  flex-direction: column;
  box-sizing: border-box;
  padding: 0;
  background-color: #fff;
}

.conversation-list-title {
  display: flex;
  align-items: center;
  box-sizing: border-box;
  height: 5.25rem; /* == .ChatHeader__header height, so the rules line up */
  padding: 0 1.5rem;
  border-bottom: 2px solid #eee;
  font-weight: 500;
  font-style: normal;
  font-size: 2.25rem;
  line-height: 120%;
  letter-spacing: -0.03em;
}

.feed-filters {
  margin: 1rem;
}

.feed-filters {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 0.25rem;
  padding: 0.25rem;
  border-radius: 100vw;
  background: #f4f4f2;
}

.feed-filters button {
  width: 100%;
  min-height: 2rem;
  padding: 0.375rem 0.5rem;
  border: 0;
  border-radius: 100vw;
  background: transparent;
  color: #575757;
  font: inherit;
  font-size: 0.8125rem;
  font-weight: 600;
  line-height: 1.25rem;
  cursor: pointer;
}

.feed-filters button[data-action="messages-filter-all"],
.feed-filters button:focus {
  background: #181818;
  color: #fff;
  box-shadow: 0 1px 2px rgba(0, 0, 0, 0.14);
}

.feed-filters:focus-within button[data-action="messages-filter-all"]:not(:focus) {
  background: transparent;
  color: #575757;
  box-shadow: none;
}

.feed-filters button:focus-visible {
  outline: 2px solid #181818;
  outline-offset: 2px;
}
</style>
