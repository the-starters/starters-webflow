# Use Claude for repository review and validation. Codex is over its usage
# limit on this machine; switch back once it resets. no-mistakes reads this
# file from the default branch, so the change has to land on main to apply.
agent: claude

ignore_patterns:
  - "*.min.js"

intent:
  enabled: true

test:
  evidence:
    store_in_repo: false
