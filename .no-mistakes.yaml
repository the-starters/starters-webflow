# Codex was previously SIGKILLed at spawn, but `no-mistakes doctor` confirmed
# it is runnable on 2026-08-08.
agent: codex

ignore_patterns:
  - "*.min.js"

intent:
  enabled: true

test:
  evidence:
    store_in_repo: false
