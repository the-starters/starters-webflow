# codex is SIGKILLed at spawn on this machine as of 2026-08-05, so the pipeline
# cannot run with it here. claude is the agent no-mistakes doctor reports runnable.
agent: claude

ignore_patterns:
  - "*.min.js"

intent:
  enabled: true

test:
  evidence:
    store_in_repo: false
