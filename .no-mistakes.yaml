# Codex review is runnable again as of 2026-08-02. Keep the validation agent
# aligned with the repository's Codex release workflow.
agent: codex

ignore_patterns:
  - "*.min.js"

intent:
  enabled: true

test:
  evidence:
    store_in_repo: false
